import { ConfigOptions } from "karma";
import { RuleSetRule, RuleSetUseItem } from "webpack";
declare type POJO = Record<string, any>;
/**
 * Does a shallow merge of two objects, only overriding values that are `undefined`.
 */
export declare function mergeConfig<T extends POJO>(target: T | undefined, defaults: Partial<T>): T;
/**
 * Adds the specified Karma plugin to the config
 */
export declare function addPlugin(config: ConfigOptions, plugin: string): ConfigOptions;
/**
 * Determines whether the specified Webpack loader already exists in the rules list.
 */
export declare function hasWebpackLoader(rules: RuleSetRule[], name: string): boolean;
/**
 * Returns the name of the given Webpack loader, if possible.
 */
export declare function webpackLoaderName(loader: RuleSetUseItem): string | undefined;
export {};
