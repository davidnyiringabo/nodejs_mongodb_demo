import { Config } from "karma";
import { Options } from "./options";
/**
 * A function that sets the Karma configuration
 */
export declare type KarmaConfig = (karma: Config) => void;
/**
 * Cretaes a Kamra configuration based on the given options.
 */
export declare function karmaConfig(options?: Options): KarmaConfig;
