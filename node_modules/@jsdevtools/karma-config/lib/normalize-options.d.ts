import { ConfigOptions, FilePattern } from "karma";
import { Options } from "./options";
/**
 * Normalized options with defaults applied.
 */
export interface NormalizedOptions {
    windows: boolean;
    mac: boolean;
    linux: boolean;
    testDir: string;
    sourceDir: string;
    CI: boolean;
    transpile: boolean;
    coverage: boolean;
    tests: Array<string | FilePattern>;
    fixtures: Array<string | FilePattern>;
    serve: Array<string | FilePattern>;
    config: ConfigOptions;
    browsers: {
        chrome: boolean;
        firefox: boolean;
        safari: boolean;
        edge: boolean;
        ie: boolean;
    };
}
/**
 * Normalizes user-specified options and applies defaults.
 */
export declare function normalizeOptions(options?: Options): NormalizedOptions;
/**
 * Wraps the given value in an array, if necessary
 */
export declare function arrayify<T>(value: T | T[] | undefined): T[] | undefined;
/**
 * Returns the boolean value of the specified environment variable.
 */
export declare function environmentFlag(name: string): boolean;
/**
 * Returns the normalized string value of the specified environment variable.
 */
export declare function environmentVariable(name: string): string;
