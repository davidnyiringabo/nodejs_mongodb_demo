import { ConfigOptions } from "karma";
import { Options } from "./options";
/**
 * Builds the Kamra configuration object based on the given options.
 */
export declare function buildConfig(options?: Options): ConfigOptions;
