"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.karmaConfig = void 0;
const nodeUtil = require("util");
const build_config_1 = require("./build-config");
/**
 * Cretaes a Kamra configuration based on the given options.
 */
function karmaConfig(options) {
    let config = (0, build_config_1.buildConfig)(options);
    return function configureKarma(karma) {
        if (config.logLevel !== karma.LOG_DISABLE) {
            console.debug("Karma Config:\n", nodeUtil.inspect(config, {
                depth: 10,
                colors: true,
                compact: false,
            }));
        }
        karma.set(config);
    };
}
exports.karmaConfig = karmaConfig;
//# sourceMappingURL=karma-config.js.map