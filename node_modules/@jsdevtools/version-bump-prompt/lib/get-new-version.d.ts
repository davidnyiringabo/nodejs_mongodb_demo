import { Operation } from "./operation";
/**
 * Determines the new version number, possibly by prompting the user for it.
 */
export declare function getNewVersion(operation: Operation): Promise<Operation>;
