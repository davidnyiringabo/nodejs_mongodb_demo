import { Operation } from "./operation";
/**
 * Commits the modififed files to Git, if the `commit` option is enabled.
 */
export declare function gitCommit(operation: Operation): Promise<Operation>;
/**
 * Tags the Git commit, if the `tag` option is enabled.
 */
export declare function gitTag(operation: Operation): Promise<Operation>;
/**
 * Pushes the Git commit and tag, if the `push` option is enabled.
 */
export declare function gitPush(operation: Operation): Promise<Operation>;
